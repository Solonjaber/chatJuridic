{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "python -c \"import os; port = os.environ.get('PORT', '8080'); os.system(f'chainlit run pdf_juri2.py --host 0.0.0.0 --port {port} --debug')\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
